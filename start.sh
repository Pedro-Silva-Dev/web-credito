# remover o container docker.
docker-compose down
# buildar e executar todos os comandos necessários para deixar o container pronto para uso.
docker-compose build
# subir o container da aplicação web front-end.
docker-compose up -d